fastapi>=0.111.0
uvicorn[standard]>=0.30.0
anthropic>=0.30.0
openai>=1.35.0
# Pin to the langchain 0.3 / langgraph 0.6 line. langchain-core 1.x exists but
# is not yet uniformly adopted by the anthropic/openai/google providers — the
# 0.3/0.2 trio below is the last consistent set. Revisit when all three hit 1.x.
langgraph>=0.2.0,<1.0
langchain-anthropic>=0.2.0,<1.0
langchain-openai>=0.2.0,<1.0
langchain-google-genai>=2.0.0,<3.0
langchain-core>=0.3.0,<1.0
redis>=5.0.0
pydantic>=2.8.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
httpx>=0.27.0
sse-starlette>=2.1.0
python-multipart>=0.0.9
pymupdf>=1.24.0
# Wave 4 — scanned-PDF OCR on Linux/prod. pytesseract drives the tesseract-ocr
# binary (installed in the agents Docker image); Pillow renders PDF pages to
# images. macOS dev additionally uses ocrmac (Apple Vision), which is optional
# and intentionally not pinned here.
Pillow>=10.0.0
pytesseract>=0.3.10
# D.0.7 — LLM observability. CallbackHandler integrates with LangChain 0.3
# and captures prompts, tool calls, token counts, latency, cost per trace.
langfuse>=2.55.0
